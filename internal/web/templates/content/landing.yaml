header:
  project_name: "Uptime Monitor"
  site_url: "https://victoriacheng15.github.io/uptime-monitor"

llms:
  objective: "A serverless uptime monitoring solution designed for personal fleet management, demonstrating AWS proficiency and SRE principles through Go, Lambda, S3-backed persistence, and scheduled EventBridge triggers."
  stack: "Go, AWS Lambda, S3, EventBridge, Terraform, Tailwind CSS"
  pattern: "Serverless Event-Driven Monitor / Scheduled Fleet Polling"
  entry_point: "cmd/lambda/main.go (Backend) / cmd/web/main.go (SSG)"
  persistence_strategy: "S3-backed JSON storage for latest fleet snapshots and URL-keyed historical trendlines (Pulse)"
  observability: "Structured logging for check failures, S3 state tracking, and UI-driven Pulse trendlines for 5-hour availability windows."

# Architecture Blueprint
architecture:
  diagram_ascii: |2
                     [1] DEPLOYMENT & CI/CD PIPELINE
                     ===============================
                            Push to main / PR
                                    │
                                    ▼
                        ┌───────────────────────┐
                        │  GitHub CI/CD Runner  │
                        └─────┬───────────┬─────┘
                              │           │
            ┌─────────────────┘           └─────────────────┐
            ▼ (Deploy Backend)                              ▼ (Deploy SSG Page)
     ┌─────────────────────────────┐                 ┌─────────────────────────────┐
     │ 1. Run Go Unit/E2E Tests    │                 │ 1. Fetch telemetry logs     │
     │ 2. Package Lambda Bootstrap │                 │    from Lambda API Endpoint │
     │ 3. Run Terraform Apply (IaC)│                 │ 2. Compile Tailwind CSS     │
     └──────────────┬──────────────┘                 │ 3. Generate static HTML     │
                    │                                └──────────────┬──────────────┘
         (Reads / Writes tfstate)                                   │
                    │                                               │
                    ▼                                               │
     ┌─────────────────────────────┐                                │ (Publishes static
     │     S3 Backend Bucket       │                                │  dist assets)
     │   (uptime-monitor-tfstate)  │                                │
     └──────────────┬──────────────┘                                ▼
                    │                                ┌─────────────────────────────┐
                    │ (Configures Lambda,            │    GitHub Pages Website     │
                    │  S3, & EventBridge)            └─────────────────────────────┘
                    ▼
     ┌─────────────────────────────┐
     │   AWS Lambda & S3 Bucket    │
     └─────────────────────────────┘

                     [2] APPLICATION FLOW
                     ====================
                     ┌───────────────────────┐
                     │      EventBridge      │
                     └───────────┬───────────┘
                                 │ (Hourly Trigger)
                                 ▼
                     ┌───────────────────────┐
          Client ───►│      AWS Lambda       │
       (HTTP Req)    │    (Go API Router)    │
                     └────┬──────┬──────┬────┘
                           │      │      │
            ┌──────────────┘      │      └──────────────┐
            ▼ (/latest)           ▼ (/check)            ▼ (/history)
       ┌──────────┐         ┌───────────┐         ┌──────────┐
       │ S3 Read  │         │ Run check │         │ S3 Read  │
       │  latest  │         │ against   │         │ history  │
       │  .json   │         │ websites  │         │  .json   │
       └──────────┘         └─────┬─────┘         └──────────┘
                                 │ (Write outputs)
                                 ▼
                           ┌───────────┐
                           │ S3 Bucket │
                           │   Store   │
                           └───────────┘

# Core Components & Logic
tech:
  - title: "AWS Lambda Execution"
    description: "AWS Lambda serves as the execution runtime, running a Go API router that handles HTTP endpoints and validates request actions."
  - title: "Amazon S3 Storage"
    description: "Stores state objects directly in Amazon S3, maintaining latest.json for current fleet status snapshots and history.json for rolling historical trend metrics."
  - title: "EventBridge Cron Trigger"
    description: "Invokes the Lambda cron path automatically every hour via Amazon EventBridge, driving background uptime checks to ensure persistent coverage."

# Validation & Resiliency Testing
proof:
  - title: "Reproducibility"
    description: "Provisioned entirely using declarative Terraform modules, ensuring reproducible environment boots, IAM role boundary containment, and consistent AWS resource setups."
  - title: "Automated Verification"
    description: "Unit tests validation with mock AWS client interfaces and testing harnesses, verifying JSON marshaling, state validation, and router responses under failure conditions."
  - title: "Telemetry Pipeline"
    description: "Structured JSON logging emitted on health-check failures, standard error output capture, and historical pulse trendlines rendered in the frontend to visualize operational availability."

# Design Trade-offs & Verification Logs
reach:
  humble_pivots:
    - title: "Serverless S3 Over Relational DB"
      description: "Chose S3 JSON storage rather than DynamoDB or RDS. This minimized running costs and database operational overhead, since fleet snapshots and short histories only require basic key-value read/write access patterns."
    - title: "Lambda Function URLs Instead of API Gateway"
      description: "Avoided API Gateway for the backend API by utilizing Lambda Function URLs. This significantly reduced infrastructure complexity, since the project did not need advanced routing, request transformation, or edge authorizers at this stage."
  objective_clarity:
    description: "Evaluated under single-writer assumptions. Up to 5 monitored endpoints with concurrent execution checks capped to 10-second request timeouts to prevent AWS Lambda execution drift and control costs."
  verifiable_outputs:
    - title: "Validation Suite (Unit Tests)"
      terminal_output: |
        === RUN   TestRouter
        === RUN   TestRouter/health_returns_healthy_response
        === RUN   TestRouter/check_accepts_post
        === RUN   TestRouter/latest_returns_stored_snapshot
        === RUN   TestRouter/history_returns_stored_entries
        --- PASS: TestRouter (0.00s)
            --- PASS: TestRouter/health_returns_healthy_response (0.00s)
            --- PASS: TestRouter/check_accepts_post (0.00s)
            --- PASS: TestRouter/latest_returns_stored_snapshot (0.00s)
            --- PASS: TestRouter/history_returns_stored_entries (0.00s)
        PASS
        ok  	uptime-monitor/internal/api	0.004s

        === RUN   TestCheck
        === RUN   TestCheck/marks_200_response_up
        === RUN   TestCheck/marks_500_response_down
        === RUN   TestCheck/records_request_error
        --- PASS: TestCheck (0.00s)
            --- PASS: TestCheck/marks_200_response_up (0.00s)
            --- PASS: TestCheck/marks_500_response_down (0.00s)
            --- PASS: TestCheck/records_request_error (0.00s)
        PASS
        ok  	uptime-monitor/internal/monitor	0.003s

        === RUN   TestStoreLatest
        === RUN   TestStoreLatest/returns_stored_latest_response
        --- PASS: TestStoreLatest (0.00s)
            --- PASS: TestStoreLatest/returns_stored_latest_response (0.00s)
        === RUN   TestStoreHistory
        === RUN   TestStoreHistory/returns_stored_history_response
        --- PASS: TestStoreHistory (0.00s)
            --- PASS: TestStoreHistory/returns_stored_history_response (0.00s)
        PASS
        ok  	uptime-monitor/internal/storage	0.004s

    - title: "Integration Suite (BDD / E2E Tests)"
      terminal_output: |
        === RUN   TestFeatures
        Feature: EventBridge Cron Scheduled Trigger
          Scenario: Scheduled EventBridge trigger invokes the checks handler
          Scenario: Missing monitoring targets prevents check execution

        Feature: HTTP Polling Client
          Scenario: Polling healthy and unhealthy endpoints
          Scenario: HTTP client handles redirect response codes as healthy status

        Feature: S3 Snapshot Persistence
          Scenario: Writing first status snapshots to empty S3 bucket
          Scenario: Appending results to existing rolling history in S3

        6 scenarios (6 passed)
        22 steps (22 passed)
        --- PASS: TestFeatures (0.01s)
        PASS
        ok  	uptime-monitor/e2e	0.015s

    - title: "Infrastructure Registry (Tofu State List)"
      terminal_output: |
        $ tofu state list
        module.events.aws_cloudwatch_event_rule.scheduled_check
        module.events.aws_cloudwatch_event_target.scheduled_check
        module.events.aws_lambda_permission.eventbridge
        module.lambda.data.aws_iam_policy_document.assume_role
        module.lambda.data.aws_iam_policy_document.history_bucket_access
        module.lambda.aws_iam_policy.history_bucket_access
        module.lambda.aws_iam_role.lambda
        module.lambda.aws_iam_role_policy_attachment.basic_execution
        module.lambda.aws_iam_role_policy_attachment.history_bucket_access
        module.lambda.aws_lambda_function.backend
        module.lambda.aws_lambda_function_url.backend
        module.lambda.aws_lambda_permission.function_url
        module.lambda.aws_lambda_permission.function_url_invoke
        module.s3.aws_s3_bucket.history
        module.s3.aws_s3_bucket.tfstate
        module.s3.aws_s3_bucket_public_access_block.history
        module.s3.aws_s3_bucket_public_access_block.tfstate
        module.s3.aws_s3_bucket_server_side_encryption_configuration.history
        module.s3.aws_s3_bucket_server_side_encryption_configuration.tfstate
        module.s3.aws_s3_bucket_versioning.history
        module.s3.aws_s3_bucket_versioning.tfstate

footer:
  author: "Victoria Cheng"
  github_link: "https://github.com/victoriacheng15"
  linkedin_link: "https://www.linkedin.com/in/victoriacheng15/"
